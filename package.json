{
  "name": "console-overlay",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build ./src --outfile=./dist/js-console-overlay.min.js --minify --target=browser",
    "watch": "bun run build -- --watch"
  },
  "devDependencies": {
    "bun-types": "latest",
    "csstype": "^3.1.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}